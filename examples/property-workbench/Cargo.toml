[package]
name = "property-workbench"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
leptos.workspace = true
rustify-components = { path = "../../crates/rustify-components" }
rustify-ui = { path = "../../crates/rustify-ui" }

[target.'cfg(target_arch = "wasm32")'.dependencies]
rustify-makepad = { path = "../../crates/rustify-makepad" }
# A cleanup has to be Send; a DOM handle is not, and never leaves this thread.
send_wrapper = "0.6"
wasm-bindgen.workspace = true
