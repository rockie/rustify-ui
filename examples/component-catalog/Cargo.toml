[package]
name = "component-catalog"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
leptos.workspace = true
rustify-components = { path = "../../crates/rustify-components" }
rustify-ui = { path = "../../crates/rustify-ui" }

[target.'cfg(target_arch = "wasm32")'.dependencies]
rustify-makepad = { path = "../../crates/rustify-makepad" }
wasm-bindgen.workspace = true
# The formatting options are the application's, and an options object is a JS
# object: this page builds the two it wants.
js-sys.workspace = true
