# 通用开发计划骨架（dev-plan 默认模板）

> 除非用户显式指定其他模板，dev-plan 一律使用本骨架，不依赖目标仓库存在模板文件。
> 按改动面与风险裁剪章节；删除不适用的占位和写作指引，不为满足形式填充内容。
> 目标读者是后续实现者，因此事实、决策、接口、迁移、失败和验收必须闭环。
> 同理，写作过程不进计划：用了哪份 skill、骨架或参考资料，读过哪些规则文件、哪些确认无约束，实现者都不需要知道。
> 仓库规则里真正约束实现的条目落成 §0.1 的 `C-*` 或 §12 的一行一坑，带规则路径与被约束的具体行为。
> 「实施者定位」「实施进度」两节是实施期的完整合同：实施者只读计划、不加载本 skill，所以实施需要的规则只能写在这两节里。两节的协议文字原样带入，其中唯一的填空是 `<进度校验命令>`；恢复快照与完成记录按真实状态初始化。

```markdown
# <代号> · <项目名> · <功能名>（<形态 · 关键架构特征>）

> **计划状态：<Ready / Proposed / Blocked>**
>
> 调查基线：<YYYY-MM-DD> · <commit/version> · <clean/dirty；dirty 时说明事实基于当前工作树>。
>
> 本期交付：**<交付物形态与核心能力>**。
> 本期独特职责：<与最接近现有能力的差异>。
> **顶层排除：<最容易被误解为在范围内的内容>**。

## 实施者定位

执行本计划的 agent 是**资深软件工程师**：Kent Beck 式的 TDD 纪律加上《程序员修炼之道》式的精确。本节与「实施进度」是实施本计划的完整约定：实施与续做只依据本计划、仓库规则和代码，不需要加载写计划用的 skill 或参考资料。开始任何里程碑前先接受以下约定。

- **表达方式**：极简，每句话都可引用。说到代码给文件路径，说到需求或验收给本计划的 ID（需求账本 R-* / NFR-* / C-* / A-*，决策 D* / ADR-*，里程碑 M*）。不写铺垫、不写感想、不复述计划。
- **完成的定义**：没有通过验证的任务不算完成。里程碑退出条件里的测试、断言和走查全部通过，才能在「实施进度」记为完成；验证没跑、失败或环境缺失，就如实记为缺口或阻塞。
- **工作顺序**：需要行为测试的改动先红、再绿、最后重构。按前置依赖推进；同一或不同里程碑中，前置已满足、契约稳定、文件与运行资源互不干扰的任务，应启动多个 subagents 并发完成。共享文件单人负责，共享数据库、端口、浏览器会话等资源须隔离或串行；工具不可用或无法安全拆分时说明原因并串行。
- **并行交付**：无特别要求时 subagent 使用与主 agent 相同的模型，启动时默认继承，不主动覆盖模型。主 agent 给每个 subagent 一份精简任务说明：目标与对应章节/ID、已稳定的契约、可写与禁改路径、可用运行资源、验收条件和独立记录路径，不转交整段聊天或无关历史。subagent 只写自己的改动和记录，边界要变先回报；完成、受阻或暂停时回报简要结论、改动路径、验证结论、缺口与记录链接。主 agent 逐项审查交付并统一集成验证，单独负责计划回写和里程碑记录；不以子任务通过代替整个里程碑验收。某任务失败时保留已交付成果，只阻塞依赖它的任务；结束对话前收回或停止仍在运行的 subagent，并记下可接续状态。
- **源码干净**：注释解释为什么，不解释是什么。源码里不出现工单或需求编号（如 `# FR-12`、`// BUG-42`）、本计划的 R-* / M* 编号、agent 工作流标记或任何规划元数据；追溯关系记在计划摘要、独立实现/验收记录和提交说明里。交付的是可直接上生产的代码：干净、最小，没有多余防御、空洞注释或重复样板这类 AI 生成痕迹。

## 实施进度（实施期持续更新）

本节是跨对话恢复的**唯一汇总入口**，协议文字不随实施改写，实施期只更新「恢复快照」和「完成记录」。续做时先读仓库规则、本节、当前任务相关正文与依赖，再核对 `git status` 和相关 diff；记录与工作树不符时先查明原因，不凭记录覆盖用户改动，也不凭代码存在推断验收已通过。只有继续未完任务、核验前置验收、排错或审查时，才按链接读取对应记录的相关部分；不要默认加载全部记录或原始日志。

**回写时机**：每完成一个里程碑，立即先保存独立实现/验收记录，再回写本节的简要进度与引用；早于报告完成、提交代码或启动依赖该成果的任务，不等整批并行任务结束。已独立运行的任务可继续。实质进展后暂停、受阻、切换任务、发现偏差或结束对话时，也须保存已有成果与缺口，刷新快照。

**一次回写**：由主 agent 先更新对应里程碑的独立记录，再同步「恢复快照」全部 7 行及「完成记录」中该里程碑的一行；状态/摘要就地更新，不追加流水。快照的「当前状态」写在做的里程碑（可并列）及最近实质进展，「下一步」写可执行动作；命令、断言清单、调试过程和改动清单只进独立记录。完成记录首次有进展时删掉占位行，每个已开始的里程碑一行：状态仅用「进行中 / 阻塞 / 已完成」，更新时间带时区，摘要 1–2 句写交付结果或已做到哪里、还差什么，记录列填相对本计划的 Markdown 链接（如 `[M1 记录](FEATURE.records/M1.md)`）。阅读规则、加载 skill 不算进展。实现偏差涉及范围、决策、接口、数据、风险或退出条件时，同时修订正文对应章节，快照只点明变化。

**独立记录**：每个里程碑一份，放在计划旁的同名 `.records/` 目录，命名 `M1.md`、`M2.md`…，有实施事实时才建；subagent 的任务详情写同目录的 `M1-前端.md` 这类任务文件，由里程碑记录链接。记录头写对应计划链接、最近更新（带时区）、状态和代码基线（验证时对应的 commit SHA；未提交写 dirty@起始SHA 并列出关键改动路径，不拿 HEAD 冒充）。「实现记录」写已交付行为、关键路径、必要决策与计划偏差，未完成时写缺口与继续动作；「验收记录」用 `退出条件 | 命令或走查步骤 | 环境/代码基线 | 结果与必要证据` 表逐条写实际结果，未运行、失败、环境缺失分开写，不预填通过。日志、截图只在有复核价值时另存并从记录链接，不复制终端流水或聊天。

**记完成的门槛**：退出条件全部通过才可记完成。验证没跑、跑红或因环境缺失跳过，就如实写进「当前状态」/「当前阻塞」，不写“基本完成”“应该可用”。

**回写后自查**：「当前进度 n/N」的 N 等于里程碑总数，n 只计状态为「已完成」的行；每个里程碑最多一行，转为已完成时将原行移至已完成记录末尾，按实际完成先后排列；「最近完成」对应最后一条已完成记录，不按编号推算；链接指向已落盘文件，完成判定有真实验收证据。每次回写后运行 `<进度校验命令>`：它只查上述结构、计数与链接存在性，不读记录正文，也不证明验收真实通过。

### 恢复快照

- 最近更新：<尚未开始；实施后写 YYYY-MM-DD HH:mm + 时区>
- 当前进度：0/<里程碑总数> 个里程碑完成
- 当前状态：<尚未开始 / 进行中 M<n>（可并列多个）及一句实质进展 / 阻塞 / 已完成>
- 最近完成：无
- 下一步：M1 · <第一个可执行动作与退出条件>
- 当前阻塞：<无；或阻塞事实、影响与解除条件>
- 代码基线：<当前核对的 commit SHA；未提交写 dirty@起始SHA；具体路径与各次验证基线只写独立记录>

### 完成记录

| Milestone | 状态 | 更新时间 | 简要记录 | 实现与验收记录 |
| --- | --- | --- | --- | --- |
| — | — | — | 尚未开始任何里程碑 | — |

<初稿保留上面的占位行，不预建空记录、不填写虚假完成事实；本条写作指引不带入计划。>

## 0. 需求、范围与决策

### 0.1 需求与约束账本

| ID | 类型 | 来源 | 内容 | 设计/验收落点 | 状态 |
| --- | --- | --- | --- | --- | --- |
| R-1 | 功能 | <用户原话> | <可观察的业务能力> | <章节 + 验收> | 已确认 |
| NFR-1 | <性能/可靠性/安全/...> | <用户/规则/代码；未知则写未知> | <目标或待确认问题> | <机制 + 验证> | 已知/未知/阻塞 |
| C-1 | 约束 | <规则/代码路径> | <技术栈/兼容/交付约束> | <影响章节> | 已确认 |
| A-1 | 假设 | <为何暂时依赖> | <假设内容> | <验证办法 + 责任人 + 最晚确认点> | 开放/阻塞 |

<只列受本需求影响的 NFR；没有来源的 p95、吞吐、SLA、RPO/RTO 或预算不得编造。>

### 0.2 决策表

| # | 决策点 | 选择 | 含义/影响 | 依据 |
| --- | --- | --- | --- | --- |
| D1 | 形态 | <选择> | <部署、既有代码、边界和零改动声明> | <R/NFR/C + 代码路径> |
| D2 | <接口/协议/权限/数据归属/...> | <选择> | <正负后果> | <证据> |

### 0.3 ADR-lite（仅高影响、难回退决策）

#### ADR-1：<标题>

- 状态：<Proposed / Accepted / Blocked>
- 背景与驱动：<关联 R/NFR/C>
- 备选：<至少两个真实可行方案及淘汰理由>
- 决策：<选择及证据>
- 正面后果：<收益>
- 负面/中性后果：<成本、运维、锁定、兼容影响>
- 重新评估触发：<规模、故障、成本或需求变化>

### 0.4 职责与事实所有权

|  | <系统 A> | <本模块/本期> | <系统 B> |
| --- | --- | --- | --- |
| 拥有 | <事实/策略/恢复责任> | <事实/策略/恢复责任> | <事实/策略/恢复责任> |
| 不拥有 | <红线> | **<绝不顺手承担的职责>** | <红线> |

一句话：<把职责与失败恢复责任切死>。

### 0.5 明确不在本期

- **<相邻功能>**——<去向：二期 / 另立项 / 依赖某能力 / 明确放弃>。

## 1. 当前事实与改动面

### 1.1 现状与缺口

- **已核实**：<现有行为>（<路径/符号>）。
- **缺口**：<现状不能满足什么> → <最小增量> → <漏做后果>（<路径/符号>）。
- **未核实阻塞**：<缺失证据> → <影响的决策> → <解除办法>。

### 1.2 拓扑与文件清单

<列出受影响的 workspace/包/模块/配置/脚本；★ 标新增。增量型同时写清无需修改的邻接面及依据。>

<仅当三个以上节点、异步时序、所有权或状态迁移难以用文字看清时加入最小图。>

## 2. 模块、接口与依赖（结构性设计时使用）

| 模块 | 调用者 | 接口与不变量 | 接缝 | 隐藏复杂度 | 依赖分类/适配器 | 测试面 |
| --- | --- | --- | --- | --- | --- | --- |
| <名称/路径> | <调用方> | <入口、顺序、错误、配置、性能> | <替换点或无> | <集中拥有的规则> | <进程内/本地可替代/远程自有/外部> | <通过接口观察的结果> |

<说明候选接口比较、删除测试，以及为什么需要或不需要新接缝。>

## 3. 数据模型与迁移（涉及持久化时使用）

| 表/对象 | 关键字段与约束 | 生命周期/权威来源 | 查询与索引 |
| --- | --- | --- | --- |
| <名称> ★ | <类型、FK/软引用、NOT NULL、UNIQUE> | <创建/更新/删除/失效/快照来源> | <查询形状 + 已有/新增索引> |

### 3.1 兼容与迁移

1. **Expand**：<向前/向后兼容的 schema/接口增量>。
2. **Migrate/Backfill**：<批次、断点、幂等、双写/回读、失败恢复>。
3. **切换读写**：<门、指标、灰度或版本窗口>。
4. **Contract**：<何时删除旧列/旧接口，前置证据>。
5. **回滚**：<代码/schema/数据/外部契约分别如何回退；不可逆点>。

## 4. 集成与契约

| 依赖/调用方 | 当前状态 | 行为级证据 | 本期处理 | 失败语义 |
| --- | --- | --- | --- | --- |
| <名称> | 已核实足够 / 已核实缺口 / 未核实阻塞 | <路径/符号 + 分页/鉴权/事务等语义> | 零改动 / 精确增量 / 阻塞解除 | <超时、重试、降级、错误> |

<需要的配置、种子、权限、scope、事件、错误码和共享导出逐项列出登记位置。>

## 5. 核心机制与服务端

- **主流程**：<编号写透状态变化、幂等短路、并发控制、失败回退和每步凭证>。
- **接口/路由**：<入口、参数、不变量、闸门顺序、业务与传输错误语义>。
- **权限链**：<UI 可见性 → token/bootstrap/载荷 → 服务端真闸>。
- **并发**：<条件写/唯一约束/事务锁；后台任务的认领、租约、僵尸恢复和计数原子性>。
- **数据权威**：<派生/快照值的服务端来源；删除、转移、失权后的行为>。

## 6. 前端与交互（涉及 UI 时使用）

- 信息架构与角色视图：<入口、主路径、权限差异>。
- 状态：<loading / empty / error / partial / disabled / success>。
- 可访问性、响应式、主题、i18n：<项目规则 + 验证>。
- 前端隐藏只改善体验，不能代替服务端授权。
- 真实浏览器走查：<项目可用工具、主路径和关键边界；环境不可用时明确未验证>。

## 7. NFR、安全与运行保障

| ID | 基线/来源 | 目标或未知项 | 超限/失败行为 | 机制 | 验证 |
| --- | --- | --- | --- | --- | --- |
| NFR-1 | <路径/用户> | <可量化目标；未知则说明影响> | <拒绝/排队/降级/告警> | <设计> | <断言/观测> |

安全项逐条对应验证：鉴权、越权矩阵、租户/用户隔离、敏感数据、注入/SSRF/XSS、限流、审计与密钥轮换。只列与本期风险相关的项。

可观测性逐条落到现有日志、指标、追踪、审计、健康检查和告警入口；不存在的能力列精确增量，不凭空承诺。

## 8. 失败模式、发布与回滚

| 失败/触发 | 爆炸半径 | 数据后果 | 用户表现/降级 | 检测 | 恢复/补偿 | 验证 |
| --- | --- | --- | --- | --- | --- | --- |
| <失败> | <租户/模块/全局> | <丢失/重复/延迟/无> | <可观察表现> | <日志/指标/告警> | <自动/人工步骤> | <故障注入/断言> |

- 发布顺序：<代码、配置、迁移、开关、依赖的顺序>。
- 放量/切换门：<必须为绿的指标与断言>。
- 回滚门：<何时回滚、由谁执行、数据如何处理>。
- 简单局部任务若无新增运行期失败模式，写明依据后可省略表格。

## 9. 验证

### 9.1 行为与接口验证

<通过公开接口验证不变量、错误、权限、幂等和状态转换；避免绑定内部实现。>

### 9.2 集成与回归

<复用/扩展哪些既有测试和脚本；跨模块契约、旧行为基线与兼容窗口如何验证。>

### 9.3 NFR 与故障注入

<容量/延迟/可靠性/恢复目标如何测；依赖超时、重复、部分失败、迁移中断如何注入。>

### 9.4 真实环境与 UI

<从登录/配置到核心结果的步骤级走查；角色、语言、主题、错误、另一租户/账户边界。>

### 9.5 静态校验

<项目实际提供的 typecheck/lint/build/schema/i18n/依赖检查命令。>

## 10. 里程碑（每步可独立验收）

| # | 里程碑 | 前置依赖 | 内容与并行边界 | 验证/退出条件 |
| --- | --- | --- | --- | --- |
| M1 | <行为基线/骨架> | <无或明确前置> | <最小改动；可独立任务与共享文件/资源归属> | <既有回归 + 新断言>；回写「实施进度」 |
| Mn | <收尾> | <依赖的里程碑/契约> | <迁移、文档、总验证；集成负责人> | <集成回归 + 真实环境 + Ready 门>；回写「实施进度」 |

<每一行的退出条件都以「回写『实施进度』」结尾，逐行写出——实施者是按行核对退出条件的，只在表下写一句总约定会被跳过。>

<并行范围说明到可写路径与运行资源即可；独立任务必须实际委派 subagents，不把“不同目录”等同于“无依赖”。共享契约先定、共享文件单人修改，最后统一集成验收。没有安全并行空间时写明具体依赖或冲突，不为并行硬拆任务。>

## 11. 风险、开放问题与就绪状态

| 项目 | 影响 | 责任人/解除办法 | 最晚确认点 | 是否阻塞 |
| --- | --- | --- | --- | --- |
| <风险/问题/A-* / NFR 未知> | <范围/安全/数据/契约/排期> | <动作> | <里程碑前> | 是/否 |

- 最终状态：**<Ready / Proposed / Blocked>**。
- 定级理由：<为什么可以实施，或哪些项阻塞>。

## 12. 已知坑与历史教训

- <一行一坑 + 规则/事故/进度文档路径；说明本计划如何规避>。

## 13. 需求 → 设计 → 验证映射

| ID | 需求/约束/假设 | 设计落点 | 验证/解除办法 | 结果 |
| --- | --- | --- | --- | --- |
| R-1 | <内容> | <章节 + 机制> | <断言/走查> | 覆盖 |
| NFR-1 | <内容> | <章节 + 机制> | <测试/观测> | 覆盖/阻塞 |
| C-1 | <内容> | <章节> | <静态/运行检查> | 覆盖 |
| A-1 | <内容> | <依赖的决策> | <验证办法> | 已解除/阻塞 |
| — | **不在本期：<汇总>** | §0.5 | <去向> | 排除 |
```

## 发出前自检

- [ ] 默认使用本文件；未探测或依赖目标仓库中的模板。
- [ ] 计划记录调查日期、commit/version 和 dirty 状态。
- [ ] 全文没有写作过程元信息：用了哪份 skill/骨架/参考、读过哪些规则文件的清单、调查步骤；相关约束已落成 `C-*` 或 §12。
- [ ] 顶部实施进度已按真实状态初始化，0/N、M1、里程碑总数和代码基线互相一致；没有预填虚假完成记录。
- [ ] 「实施进度」的回写协议为骨架原文在位，`<进度校验命令>` 已替换；每个里程碑的退出条件都以「回写『实施进度』」结尾。
- [ ] 计划只留简要进度与记录链接；详情按里程碑独立保存、按需读取，进行中/阻塞不计完成。
- [ ] 依赖和并行边界明确；无冲突任务使用 subagents，共享文件/资源单人负责或隔离，主 agent 集成验收并单写计划。
- [ ] 顶部“实施者定位”为骨架原文，未改写、未删减。
- [ ] R/NFR/C/A 全部映射；未知数字未被伪造成目标。
- [ ] 关键决策有真实备选、正负后果和重新评估触发；普通选择未被 ADR 化。
- [ ] 复杂设计写清模块、接口、接缝、依赖分类与测试面；没有假想抽象。
- [ ] 契约均为“已核实足够 / 已核实缺口 / 未核实阻塞”之一。
- [ ] 数据约束与生命周期、迁移、兼容和回滚互相一致。
- [ ] 失败模式的检测、恢复和验证在改动面与里程碑中有落点。
- [ ] 验证通过接口观察行为；安全/NFR 条目均可断言或观测。
- [ ] 复用/已核实路径真实存在，新增路径已明确标注。
- [ ] Ready 计划没有影响范围、安全、数据、外部契约或关键 NFR 的未决项。
