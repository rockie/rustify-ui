# M3 · Tailwind 工具链：mise 二进制与精确扫描 · 实现与验收记录

- 对应计划：[DX-REFINE](../../plan/DX-REFINE.md) M3（R-6、NFR-3、D10、D11、A-1）
- 最近更新：2026-09-25 UTC
- 状态：见计划「完成记录」
- 代码基线：dirty@`65b2225`（与 M1 同一工作树）

## 实现记录

- 子任务交付与本地验收详情：[m3-toolchain.md](m3-toolchain.md)。要点：`mise.toml` 以显式 `github:tailwindlabs/tailwindcss` 后端固定 4.1.13；`xtask/src/tailwind.rs`（`RUSTIFY_TAILWIND` → PATH，校验 `--help` 横幅版本，`compile(input, output, minify)`）；`css.rs` 改用它、`--check` 打印首个差异行；SDK 输入加 `source(none)`（产物逐字节不变，构建 0.82–0.95 s → 0.32–0.36 s）；删除 npm 的 Tailwind 依赖与 `css` 脚本；`doctor` 报告二进制与版本。
- 主 agent 集成（与 M1 共用的文件，按并行说明在 M1 之后改）：`xtask/src/verify.rs` 加 `css --check`、`catalog --check` 两步；`.github/workflows/verify.yml` host job 去掉 `setup-node` 与 `npm ci`，CSS 检查直接用 mise 装的二进制；`CLAUDE.md`「Toolchain」登记 Tailwind 4.1.13 与 `RUSTIFY_TAILWIND`，并写明生成 CSS 不需要 Node。
- 缺口：A-1（mise 在 macOS arm64 上选中裸二进制并以 `tailwindcss` 暴露、macOS 产物与 linux 逐字节一致）只能在 CI 上核实——本容器的 mise 访问 GitHub API 返回 403（会话网络策略）。继续动作：推送后看 CI host job 的「Rust, mbx and Tailwind from mise.toml」与「Component stylesheet is in step with the classes」两步。

## 验收记录

| 退出条件 | 命令或步骤 | 环境/代码基线 | 结果 |
| --- | --- | --- | --- |
| `css --check` 无漂移（无 `node_modules`） | 见子任务记录 | Linux x64，独立二进制 4.1.13 | 通过（26,644 B，npm 的 Tailwind 包已移除，Node 不在 PATH 也通过） |
| 新单测通过 | `mbx test --workspace --lib --bins`（含 `mbx test -p xtask` 的 31 项） | 同上，M1 集成后 | 通过 |
| 缺二进制 / 错版本两种故障注入 | 见子任务记录 | 同上 | 均给出指向 `mise install` 的错误；4.1.12 横幅被拒 |
| `doctor` 显示版本 | `mbx xtask doctor` | 同上 | `ok tailwind tailwindcss v4.1.13 at /root/.local/bin/tailwindcss` |
| `verify` 含 CSS/目录检查 | `xtask/src/verify.rs` 审阅；`mbx build -p xtask`、clippy | 同上 | 已加入；编译与 clippy 通过（`verify` 全量运行留到 M5/M8） |
| CI host job 不再为 CSS 跑 `npm ci` 且全绿；A-1 | 推送后的 CI | macOS-14 | 待 CI |
